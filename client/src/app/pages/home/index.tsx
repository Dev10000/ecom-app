import React from 'react';
import Hero from './hero';

const Home: React.FC = (): JSX.Element => {
    return (
        <div>
            <Hero />
            Home page
        </div>
    );
};

export default Home;
